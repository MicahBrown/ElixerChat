<div class="canvas">
  <div class="chat-menu-wrapper">
    <div class="chat-menu">
      <div class="container">
        <a class="chat-share-link"><i class="fa fa-share-alt"></i><span class="show-for-medium"> Share</span></a>
        <a class="chat-users-count"><i class="fa fa-user-group"></i></a>
        <div id="clock" class="clock" data-deadline="<%= deadline(@chat) %>" title="Time left for this chat room to be active">
          <div class="face">
            <i class="fa fa-clock-o"></i>
            <span class="hours">00</span>:<span class="minutes">00</span>:<span class="seconds">00</span>
          </div>
          <div class="inner"></div>
          <!--a href="<%#= root_path(@conn, :index) %>">1DayChat</a-->
        </div>
      </div>
    </div>
  </div>
  <div class="fixed-container" style="background: #fff;">
    <div class="gutter-highlight"></div>
    <ul class="chat" id="chat"
                     data-user-auth-key="<%= Phoenix.Token.sign(@conn, "user_auth_key", @current_user.auth_key) %>"
                     data-chat-auth-key="<%= Phoenix.Token.sign(@conn, "chat_auth_key", @chat.auth_key) %>"
                     data-chat-token="<%= @chat.token %>"
                     style="padding-bottom: 12.5rem;">
      <%= for message <- @messages do %>
        <%= render Daychat.MessageView, "message.html", message: message, participants: @participants %>
      <% end %>
    </ul>
  </div>

  <div class="actions">
    <%= if Mix.env == :dev do %>
      <div style="position: absolute; right: 1rem; top: 1rem;" onclick="this.parentNode.remove()">x</div>
    <% end %>
    <div class="container">
      <%= form_for @message_changeset, chat_message_url(@conn, :create, @chat.token), [id: "message-form"], fn f -> %>
        <%= submit "Send", class: "u-pull-right", style: "height: 64px; margin: 0px; padding: 0 20px;" %>
        <div style="overflow: hidden; padding-right: 1rem;">
          <%= textarea f, :body, style: "width: 100%; resize: none; margin: 0;", id: "message-body" %>
        </div>
     </div>
      <% end %>
    </div>
  </div>

  <div class="modal" id="chat-users-modal">
    <h1>Online Chat Participants</h1>
    <div class="chat-users">
      <ul class="chat-users-list" class="list-unstyled">
        <li>Loading online users...</li>
      </ul>
    </div>
  </div>

  <div class="modal" id="share-modal">
    <h1>Share This Chat</h1>

    <div><i class="fa fa-link"></i> Link</div>
    <button id="url-button" type="submit" class="u-pull-right" data-clipboard-target="#chat-url">Copy</button>
    <div style="overflow: hidden; padding-right: .5em;">
      <input type="text" value="<%= chat_url(@conn, :show, @chat.token) %>" style="width: 100%;" id="chat-url" />
    </div>

    <div><i class="fa fa-tag"></i> Token (entered on the homepage)</div>
    <button id="token-button" type="submit" class="u-pull-right" data-clipboard-target="#chat-token">Copy</button>
    <div style="overflow: hidden; padding-right: .5em;">
      <input type="text" value="<%= @chat.token %>" style="width: 100%;" id="chat-token" />
    </div>
  </div>
</div>
<style>
.actions {
  position: fixed;
  z-index: 999;
  width: 100%;
  left: 0;
  bottom: 0;
  background: #5193ff;
  /*box-shadow: 0 -5px 5px -5px rgba(0,0,0,0.40);*/
}
.actions form {
  padding: 1rem 0;
  margin: 0;
}
.actions form textarea { border: 2px solid #555; }
.actions form button {
  border: 0;
  background: #555;
  color: #fff;
}
.chat {
  margin: 0;
  list-style-type: none;
  padding: 1rem 0;
}
.message {
  margin: 0;
}
.message.bot .message-content {
  color: #999;
  font-size: 1.25rem;
}
.message.bot:not(.first) .message-body { margin-top: 0.5rem; }
.message .message-details,
.message .message-content { padding-left: 1rem; padding-right: 9rem; width: 100%; }
.message .message-details {
  position: relative;
  color: #333;
  font-size: 1.25rem;
  position: relative;
}
.message.first .message-author { margin-top: 0; }
.message .message-author {
  font-weight: bold;
  margin-top: 0.75rem;
  width: 100%;
}
.message .message-gutter {
  position: absolute;
  left: 0;
  top: 100%;
  text-align: right;
  width: 100%;
  padding-right: 1rem;
}
.message .message-body {
  margin: 0;
  overflow-wrap: break-word;
  hyphens: auto;
}
.message .message-body p { margin: 0; }

.chat-menu-wrapper {
  position: relative;
  width: 100%;
  height: 28px;
}
.chat-menu-wrapper .chat-menu {
  line-height: 28px;
  position: fixed;
  left: 0;
  top: 0;
  z-index: 1001;
  height: 28px;
  width: 100%;
  background: #5193ff;
  /*box-shadow: 0 5px 5px -5px rgba(0,0,0,0.40);*/
}
.chat-menu a { color: #fff; cursor: pointer; display: inline-block; padding: 0 1rem; }
.chat-menu a:hover { color: #eee; cursor: pointer; }
.chat-menu .container > a:first-child { background: #555; }

.chat-menu .clock {
  float: right;
  position: relative;
  color: #fff;
}
.chat-menu .clock .face {
  position: relative;
  z-index: 501;
  padding: 2.5px 1.5rem;
  font-weight: bold;
}
.chat-menu .clock .inner {
  position: absolute;
  z-index: 500;
  right: 0;
  top: 0;
  height:103%;
  width: 100%;
  font-weight: bold;
  background: #5193ff;
  border-radius: 0 0 11px 11px;
}
.canvas { background: #ededff; }
/*.chat-menu-wrapper .chat-menu.clone {
  position: fixed;
  top: -100px;
  left: 0;
  right: 0;
  height: 28px;
  transition: 0.2s top cubic-bezier(.3,.73,.3,.74);
}

body.down .chat-menu-wrapper .chat-menu.clone {
  top: 0; }*/

.gutter-highlight {
  width: 7.75rem;
  position: absolute;
  right: 0;
  top: 0;
  height: 100%;
  background: #f6f6ff;
}

.modal { display: none; }
.modal-wrapper .modal {
  display: block;
}

.modal-close {
  cursor: pointer;
  position: absolute;
  right: 1rem;
  top: 0;
}

.modal-wrapper {
  overflow-y: auto;
  display: none;
  color: #fff;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  background: rgba(0,0,0,0.8);
  padding-top: 5rem;
  height: 100%;
  width: 100%;
}
.modal-wrapper.is-visible {
  display: block;
}

.modal-wrapper button,
.modal-wrapper input,
.modal-wrapper optgroup,
.modal-wrapper select,
.modal-wrapper textarea { color: #555; }

.modal-wrapper button { background: #555; color: #fff; }

.user-count {
  animation: blinker 1s linear;
}

@keyframes blinker {
  35% { opacity: 0; }
}
</style>

<link rel="stylesheet" href="//cdn.jsdelivr.net/highlight.js/9.9.0/styles/default.min.css">
<script src="//cdn.jsdelivr.net/highlight.js/9.9.0/highlight.min.js"></script>
